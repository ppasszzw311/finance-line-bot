# Zeabur Configuration
name: finance-line-bot

services:
  - name: app
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}
    environment:
      - PYTHON_VERSION=3.11
      - PORT=8000

# Database service will be added via Zeabur dashboard
